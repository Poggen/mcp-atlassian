# Kubeconfig setup

```bash
drv kubeconfig setup
```

With explicit org/repo:
```bash
drv kubeconfig setup --org <org> --repo <repo>
```

Prerequisites:
- `drv auth` completed
- `kubelogin` installed
