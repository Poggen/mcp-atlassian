name: On Pull Request To Main

on:
  pull_request_target:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  pr-check:
    uses: WirelessCar-WDP/drive-actions/.github/workflows/pr-go-apps.yaml@v2.12.0
    secrets: inherit
