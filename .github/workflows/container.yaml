name: On Push To Main

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  publish-updated-apps:
    uses: WirelessCar-WDP/drive-actions/.github/workflows/publish-go-apps.yaml@v2.12.0
    secrets: inherit
