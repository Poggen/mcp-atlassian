name: CodeQL Analysis

on:
  push:
    branches:
      - main
  pull_request_target:
    branches:
      - main

permissions:
  id-token: write
  contents: read
  actions: read
  packages: read
  security-events: write

jobs:
  codeql-analysis:
    uses: WirelessCar-WDP/drive-actions/.github/workflows/codeql-go.yaml@v2.12.0
    secrets: inherit
